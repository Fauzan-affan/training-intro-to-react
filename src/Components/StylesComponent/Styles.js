import styles from "../../styles.module.css"

const Styles = (props) => {
    // let textXl = props.textXl ? "text-xl" : ""
    return (
        <div className={styles.success}>Styled Element</div>
    )
}

export default Styles